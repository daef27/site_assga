{
  "installCommand": "python -m pip install -r requirements.txt",
  "buildCommand": "python django_assga/manage.py collectstatic --noinput",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.py"
    }
  ]
}
